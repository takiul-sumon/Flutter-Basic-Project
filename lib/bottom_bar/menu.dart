import 'package:flutter/material.dart';

class MenuScrean extends StatelessWidget{
  const MenuScrean({super.key});
  @override
  Widget build(BuildContext context)
  {
    return Scaffold(body: Center(
      child:Text("Menu Screan"),
    ),);
  }
}